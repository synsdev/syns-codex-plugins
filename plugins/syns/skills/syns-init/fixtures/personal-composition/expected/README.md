# Expected proposal behavior

- Run context discovery with `--without-syns`; this synthetic fixture must not inspect private account repositories.
- Select the pinned personal tasks/goals and habits/routines category-native references.
- Treat one repository versus linked repositories as an information/access boundary; the fixture asks
  for one, so show a deliberate collision-free composition.
- Preserve direct source Markdown and typed frontmatter.
- Do not add scripts, tests, generated views, JSON projections, caches or interface code to the wiki.
- Keep one canonical owner for each fact and compute Today, streaks, review summaries and other views
  in memory.
- Seal and use the three `matched_read_only_tasks` in `evals/personal-composition.json` for the later
  A/B plan; do not invent proposal-selection prompts as evaluation tasks.
